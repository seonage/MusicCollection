extends layout

block content
  h1 #{album.title}
  
  p #[strong Artist:]
    a(href = album.artist.url)  #{album.artist.artist_name}
  
  hr
  p 
    a(href = album.url + '/delete') Delete album
    
  p 
    a(href = '/album') Return to list of albums